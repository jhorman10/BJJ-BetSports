name: CI

on:
  push:
    branches: ["develop", "main", "feat/*"]
  workflow_dispatch:

jobs:
  backend-lint-and-types:
    name: "Backend: Lint + Types"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install backend deps
        run: python -m pip install -r requirements.txt

      - name: Ruff check
        run: python -m ruff check src tests

      - name: Black check
        run: python -m black --check src tests

      - name: Isort check
        run: python -m isort --check-only src tests

      - name: Mypy (Full Check)
        run: python -m mypy src --ignore-missing-imports --follow-imports=skip

  backend-tests:
    name: "Backend: Tests"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-worker.txt

      - name: Install backend deps
        run: python -m pip install -r requirements.txt -r requirements-worker.txt

      - name: Run pytest
        run: env PYTHONPATH="$GITHUB_WORKSPACE/backend" python -m pytest -v --tb=short

  frontend-lint:
    name: "Frontend: Lint + Build"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  frontend-tests:
    name: "Frontend: Tests"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend deps
        run: npm ci

      - name: Run tests
        run: npx vitest run

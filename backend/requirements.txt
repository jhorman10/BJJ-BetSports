# =============================================================================
# LIGHTWEIGHT API SERVER DEPENDENCIES
# This file is optimized for production (Render) with API_ONLY_MODE=true
# Heavy ML dependencies are in requirements-worker.txt for GitHub Actions
# =============================================================================

# Core FastAPI dependencies
fastapi==0.141.1
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.14.2

# starlette no longer pinned <1.0.0 by FastAPI >= 0.128 — get the CVE fixes
starlette>=1.3.1,<2.0.0

# Rate limiting for admin endpoints
slowapi==0.1.9

# HTTP clients (for fetching live match data)
httpx==0.26.0
aiohttp==3.14.3

# Database (lightweight ORM for reading data)
sqlalchemy==2.0.45
psycopg2-binary==2.9.11  # PostgreSQL driver (required for DATABASE_URL connection)
pymongo==4.9.1  # MongoDB driver (required by tests)
motor==3.6.0  # Async MongoDB driver (Motor) for async repository

# Environment management
python-dotenv==1.2.2

# Timezone handling
pytz==2024.1

# JSON serialization (faster than standard json)
# orjson removed — not used in codebase

# Caching (for ephemeral cache layer)
# NOTE: diskcache 5.6.3 has PYSEC-2026-2447 (pickle deserialization risk) — no fix released yet.
# Mitigation: only store trusted data in cache; consider migrating to redis for production.
diskcache==5.6.3

# Data processing (minimal - required for parsing)
numpy>=1.26.4
pandas>=2.2.0
scikit-learn>=1.5.0

joblib==1.4.2

# =============================================================================
# DEVELOPMENT DEPENDENCIES (optional, comment out for production)
# =============================================================================

# Testing
pytest==9.0.3
pytest-cov==4.1.0
pytest-asyncio==0.23.3

# Linting
ruff==0.4.4
isort==5.13.2
black==26.3.1
flake8==7.0.0
mypy==1.8.0
types-pytz==2026.1.1.20260408

# =============================================================================
# NOTES:
# - For local development with ML, use: pip install -r requirements-worker.txt
# =============================================================================


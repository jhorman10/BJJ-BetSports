# =============================================================================
# LIGHTWEIGHT API SERVER DEPENDENCIES
# This file is optimized for production (Render) with API_ONLY_MODE=true
# Heavy ML dependencies are in requirements-worker.txt for GitHub Actions
# =============================================================================

# Core FastAPI dependencies
fastapi==0.127.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0

# HTTP clients (for fetching live match data)
httpx==0.26.0
aiohttp==3.13.2

# Database (lightweight ORM for reading data)
sqlalchemy==2.0.45
psycopg2-binary==2.9.9  # PostgreSQL driver (required for DATABASE_URL connection)

# Environment management
python-dotenv==1.0.0

# Timezone handling
pytz==2024.1

# JSON serialization (faster than standard json)
orjson

# Caching (for ephemeral cache layer)
diskcache==5.6.3

# =============================================================================
# DEVELOPMENT DEPENDENCIES (optional, comment out for production)
# =============================================================================

# Testing
pytest==7.4.4
pytest-cov==4.1.0
pytest-asyncio==0.23.3

# Linting
black==23.12.1
flake8==7.0.0
mypy==1.8.0

# =============================================================================
# NOTES:
# - pandas, numpy, scikit-learn, scipy removed (saves ~300-400MB RAM)
# - joblib removed (only needed for ML model loading)
# - apscheduler removed (worker runs in GitHub Actions)
# - For local development with ML, use: pip install -r requirements-worker.txt
# =============================================================================

